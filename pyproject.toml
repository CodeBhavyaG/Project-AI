[project]
name = "project-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy<2",
    "openai>=2.41.0",
    "python-dotenv>=1.2.2",
    "torch==2.2.2",
    "transformers<5.0.0",
]
